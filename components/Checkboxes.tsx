import { Checkbox, CheckboxGroup } from "@chakra-ui/react"
import React from "react";

const Checkboxes = (event, props) => {
	const [checkedItems, setCheckedItems] = React.useState([false, false]) 
	return (
    <Checkbox>
      
    </Checkbox>
	)
}


export default Checkboxes;
